[project]
name = "my-budget-bot"
version = "0.1.0"
requires-python = ">=3.12"

dependencies = [
    "python-telegram-bot==20.7",
    "gspread==6.0.2",
    "google-auth==2.27.0",
    "fastapi",
    "uvicorn"
]

[tool.vercel]
entrypoint = "api.bot:app"
